# TODO

- [ ] Smooth web client gateway connection — see `feat/gateway-config` branch (pairing approval UI). Revisit for triggering auth flow directly from the browser client without needing the OpenClaw dashboard.
